{
  "description": "CertificateKeystores configures additional keystore output formats to be created in the Certificate's output Secret.",
  "type": "object",
  "properties": {
    "jks": {
      "description": "JKS configures options for storing a JKS keystore in the `spec.secretName` Secret resource.",
      "$ref": "_definitions.json#/definitions/io.cert-manager.pkg.apis.certmanager.v1.JKSKeystore"
    },
    "pkcs12": {
      "description": "PKCS12 configures options for storing a PKCS12 keystore in the `spec.secretName` Secret resource.",
      "$ref": "_definitions.json#/definitions/io.cert-manager.pkg.apis.certmanager.v1.PKCS12Keystore"
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}