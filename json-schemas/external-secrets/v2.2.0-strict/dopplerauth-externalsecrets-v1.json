{
  "description": "DopplerAuth configures authentication with the Doppler API. Exactly one of secretRef or oidcConfig must be specified.",
  "type": "object",
  "properties": {
    "oidcConfig": {
      "description": "OIDCConfig authenticates using Kubernetes ServiceAccount tokens via OIDC.",
      "$ref": "_definitions.json#/definitions/io.external-secrets.apis.externalsecrets.v1.DopplerOIDCAuth"
    },
    "secretRef": {
      "description": "SecretRef authenticates using a Doppler service token stored in a Kubernetes Secret.",
      "$ref": "_definitions.json#/definitions/io.external-secrets.apis.externalsecrets.v1.DopplerAuthSecretRef"
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}