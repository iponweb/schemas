{
  "description": "GCPSMProvider Configures a store to sync secrets using the GCP Secret Manager provider.",
  "type": "object",
  "properties": {
    "auth": {
      "description": "Auth defines the information necessary to authenticate against GCP",
      "default": {},
      "$ref": "_definitions.json#/definitions/io.external-secrets.v1.GCPSMAuth"
    },
    "location": {
      "description": "Location optionally defines a location for a secret",
      "type": [
        "string",
        "null"
      ]
    },
    "projectID": {
      "description": "ProjectID project where secret is located",
      "type": [
        "string",
        "null"
      ]
    },
    "secretVersionSelectionPolicy": {
      "description": "SecretVersionSelectionPolicy specifies how the provider selects a secret version when \"latest\" is disabled or destroyed. Possible values are: - LatestOrFail: the provider always uses \"latest\", or fails if that version is disabled/destroyed. - LatestOrFetch: the provider falls back to fetching the latest version if the version is DESTROYED or DISABLED",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}