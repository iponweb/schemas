{
  "description": "NgrokVault configures the ngrok vault to sync secrets with.",
  "type": "object",
  "required": [
    "name"
  ],
  "properties": {
    "name": {
      "description": "Name is the name of the ngrok vault to sync secrets with.",
      "type": [
        "string",
        "null"
      ],
      "default": ""
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}