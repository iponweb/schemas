{
  "description": "SecretStoreRetrySettings defines configuration for retrying failed requests to the provider.",
  "type": "object",
  "properties": {
    "maxRetries": {
      "description": "MaxRetries is the maximum number of retry attempts.",
      "type": [
        "integer",
        "null"
      ],
      "format": "int32"
    },
    "retryInterval": {
      "description": "RetryInterval is the interval between retry attempts.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}