{
  "description": "CacheConfig configures client-side caching for read operations.",
  "type": "object",
  "properties": {
    "maxSize": {
      "description": "MaxSize is the maximum number of secrets to cache. When the cache is full, least-recently-used entries are evicted.",
      "type": [
        "integer",
        "null"
      ],
      "format": "int32"
    },
    "ttl": {
      "description": "TTL is the time-to-live for cached secrets. Format: duration string (e.g., \"5m\", \"1h\", \"30s\")",
      "$ref": "_definitions.json#/definitions/io.k8s.apimachinery.pkg.apis.meta.v1.Duration"
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}