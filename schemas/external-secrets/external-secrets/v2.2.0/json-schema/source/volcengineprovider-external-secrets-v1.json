{
  "description": "VolcengineProvider defines the configuration for the Volcengine provider.",
  "type": "object",
  "required": [
    "region"
  ],
  "properties": {
    "auth": {
      "description": "Auth defines the authentication method to use. If not specified, the provider will try to use IRSA (IAM Role for Service Account).",
      "$ref": "_definitions.json#/definitions/io.external-secrets.v1.VolcengineAuth"
    },
    "region": {
      "description": "Region specifies the Volcengine region to connect to.",
      "type": [
        "string",
        "null"
      ],
      "default": ""
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}