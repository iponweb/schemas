{
  "description": "DelineaProviderSecretRef is a secret reference containing either a direct value or a reference to a secret key.",
  "type": "object",
  "properties": {
    "secretRef": {
      "description": "SecretRef references a key in a secret that will be used as value.",
      "$ref": "_definitions.json#/definitions/io.external-secrets.v1.SecretKeySelector"
    },
    "value": {
      "description": "Value can be specified directly to set a value without using a secret.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}