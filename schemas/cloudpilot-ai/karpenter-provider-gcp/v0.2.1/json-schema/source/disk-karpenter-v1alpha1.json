{
  "type": "object",
  "properties": {
    "boot": {
      "description": "Indicates that this is a boot disk.",
      "type": [
        "boolean",
        "null"
      ],
      "default": false
    },
    "category": {
      "description": "The category of the disk (e.g., pd-standard, pd-balanced, pd-ssd, pd-extreme).",
      "type": [
        "string",
        "null"
      ]
    },
    "kmsKeyName": {
      "description": "KMSKeyName is the Cloud KMS key to use for disk encryption. Format: projects/{project}/locations/{region}/keyRings/{keyring}/cryptoKeys/{key} Optionally, you can specify a version: projects/{project}/locations/{region}/keyRings/{keyring}/cryptoKeys/{key}/cryptoKeyVersions/{version}",
      "type": [
        "string",
        "null"
      ]
    },
    "kmsKeyServiceAccount": {
      "description": "KMSKeyServiceAccount is the service account to use for accessing the KMS key. If not specified, the Compute Engine default service account will be used.",
      "type": [
        "string",
        "null"
      ]
    },
    "secondaryBootImage": {
      "description": "SecondaryBootImage is the secondary boot disk image name (e.g. global/images/DISK_IMAGE_NAME).",
      "type": [
        "string",
        "null"
      ]
    },
    "secondaryBootMode": {
      "description": "SecondaryBootMode is the secondary boot disk mode (e.g. CONTAINER_IMAGE_CACHE).",
      "type": [
        "string",
        "null"
      ]
    },
    "sizeGiB": {
      "description": "SizeGiB is the size of the disk. Unit: GiB",
      "type": [
        "integer",
        "null"
      ],
      "format": "int32",
      "default": 0
    }
  },
  "additionalProperties": false,
  "$schema": "http://json-schema.org/schema#"
}